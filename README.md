# EventsApp-NodeBackend

## Installation

```
$ git clone git@github.com:prSathan/EventsApp-NodeBackend.git
$ cd projectdirectory
$ npm install
$ npm start
```
Please install the CORS google chrome extension to allow for data to be sent between tabs.
